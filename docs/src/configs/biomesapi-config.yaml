metrics:
  default: "true"
  description: Whether to enable FastStats metrics collection for BiomesAPI.

forced-injector:
  default: NONE
  description: >-
    BiomesAPI may force a specific packet handler implementation to be used by all
    plugins that hook into it. If a plugin attempts to use another injector, BiomesAPI
    will silently redirect it to the injector defined below.
    Options are: `NETTY`, `PROTOCOLLIB`, `PACKETEVENTS`, or `NONE`.